"""Pipeline modules for each application/modality.

Application entry points: t2i (text-to-image), molecule (QM9), protein (Proteina).
Shared utilities: common.py (problem building, device, seed, solver/reward names);
model_summary.py (config summary and parameter inspection).
"""

