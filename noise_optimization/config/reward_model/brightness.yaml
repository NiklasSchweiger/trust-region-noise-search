_partial_: true

# Brightness reward - rewards images based on average brightness
# Registered in the reward function registry
# Usage: reward_model=brightness
reward:
  name: brightness
  device: cuda
  dtype: float32

