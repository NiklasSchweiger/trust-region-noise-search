_partial_: true

# Redness reward - rewards images based on the ratio of red channel to total intensity
# Registered in the reward function registry
# Usage: reward_model=redness
reward:
  name: redness
  device: cuda
  dtype: float32

