_partial_: true

# Sharpness reward - rewards images based on sharpness (edge detection via Laplacian filter)
# Registered in the reward function registry
# Usage: reward_model=sharpness
reward:
  name: sharpness
  device: cuda
  dtype: float32

