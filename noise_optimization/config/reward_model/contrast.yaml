_partial_: true

# Contrast reward - rewards images based on standard deviation of pixel values
# Registered in the reward function registry
# Usage: reward_model=contrast
reward:
  name: contrast
  device: cuda
  dtype: float32

